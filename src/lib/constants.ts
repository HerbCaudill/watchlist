/** The current version of cached enriched media items. */
export const MEDIA_ITEM_CACHE_VERSION = 1

/** The enriched media item TTL in milliseconds. */
export const MEDIA_ITEM_CACHE_TTL_MS = 30 * 24 * 60 * 60 * 1_000

/** The current version of cached Discover result lists. */
export const DISCOVER_LIST_CACHE_VERSION = 1

/** The Discover list TTL in milliseconds. */
export const DISCOVER_LIST_CACHE_TTL_MS = 24 * 60 * 60 * 1_000
